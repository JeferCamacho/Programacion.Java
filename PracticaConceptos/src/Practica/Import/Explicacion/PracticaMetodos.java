package Import.Explicacion;

/*
En cual realiza el llamado de las clase y los metodos en para hacer uso de la collection


 */
import java.util.ArrayList;
import java.util.List;

public class PracticaMetodos {

    /*
    
    Como se puede visualizar Al utlizar las palabras < List > < ArrayList> en cual tiene unas clase que realizan
    unos comportamientos (metodo)en cual se usan para la respectiva collection
    
     */
    private List<Integer> numeros = new ArrayList();

    public void rellenarCollection() {

        //Metodo add
        numeros.add(2);

        /*
        
        numeros.addAll(numeros)
        numeros.equals(this)
        numeros.hashCode()
        numeros.addAll(0, numeros)
                
     
        
        Estos son unos de cuantos de los metodos que tiene coleccion quien puede usarce si realizamos 
        el llamado corespondiente de la carpeta donde esta ubicado las clase que le dan comportamiento
        a los metodos
        
        
         */
    }

    /*
    
    //Uso
    
    Principalmente java son metodos en cual lo que realizan organizarlos de manera jerarquica en cual los 
    subdividen en subcarpetas ese contenido que esta dentro de esas carpetas son metodos en cual con ese 
    estructuramiento podemos hacer el llamado al metodo que  queremos utlizar debemos hacer el llamado de sus
    carpetas que dentro estan los metodos que vamos a utlizar con la palabra <import> esto que realiaza el 
    llamado de las carpetas ulizarlo
    
    
    //Conclusion 
    
    Principalmente es todo metodos en cual toda visualizar enque carpeta o gerarquia esta hubicada para realizar
    el llamado y poder utlizar los metodos respectivos de las clases en la gerarquie de paquetes 
    
    
    
    
     */
}
